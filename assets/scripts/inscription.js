//  'use strict';

//  let btn = document.getElementById("btn");
//  let username = document.getElementById("username");
//  let password = document.getElementById("password");
//  let email = document.getElementById("email");
//  let msgError = document.getElementById("messageErreur");

 
//  btn.addEventListener('click', () => {console.log("bonjour");
//     let usernameVal = username.value.trim();
//     let passwordVal = password.value.trim();
//     let emailVal = email.value.trim();
//     console.log(usernameVal, passwordVal, emailVal);

//     if (usernameVal == ""  && usernameVal.length > 50 && usernameVal.length < 3) {
//             msgError.innerHTML = "Invalid username";
//         }
      
    
//     //verification de l'email

//     if(emailVal == "" || emailVal.length > 50 || emailVal.length < 5) {
//           msgError.innerHTML = "Invalid email";
//         }

//     //verification du mot de passe
//     if (empty(passwordVal) || passwordVal.length > 50 || passwordVal.length < 8) {
//         msgError.innerHTML = "Invalid password";
//     }

//  })
 
